/* Generated By:JJTree: Do not edit this line. ASTCharacterDescriptor.java */

package kiev.parser;

import kiev.Kiev;

public class ASTCharacterDescriptor extends SimpleNode {

	public boolean		not;
	public Character	char1;
	public Character	char2;

	public ASTCharacterDescriptor(int id) {
		super(0);
	}
	
	public void set(Token t) {
		if( char1 == null ) {
			if( t.image.length() == 3 )
				char1 = Kiev.newCharacter( t.image.charAt(1) );
			else {
				char1 = Kiev.newCharacter( ASTConstExpression.source2ascii(t.image.substring(1,t.image.length()-1)).charAt(0) );
			}
		} else {
			if( t.image.length() == 3 )
				char2 = Kiev.newCharacter( t.image.charAt(1) );
			else {
				char2 = Kiev.newCharacter( ASTConstExpression.source2ascii(t.image.substring(1,t.image.length()-1)).charAt(0) );
			}
		}
	}
}
